# .env.example - 环境变量配置示例
#
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 在 .env 中填入你的实际凭证
# 3. 不要将 .env 提交到 Git（已在 .gitignore 中）
#
# 获取 Zotero API Key: https://www.zotero.org/settings/keys

# ========== 必需配置 ==========
# Zotero Library ID（在设置页面顶部显示）
ZOTERO_LIBRARY_ID=your_library_id

# Zotero API Key（创建时勾选"允许写入访问"）
ZOTERO_API_KEY=your_api_key

# 临时集合 KEY（用于 search_papers.py）
# 获取方法：在 Zotero 网页版打开集合，从 URL 复制 KEY
TEMP_COLLECTION_KEY=your_temp_collection_key

# ========== 可选配置 ==========
# 启用 ChinaXiv 来源（同时检索中文预印本）
ENABLE_CHINAXIV=false

# 启用双语配置模式（auto_collect.py 使用）
USE_BILINGUAL_CONFIG=true

# 双语配置文件路径
BILINGUAL_CONFIG_PATH=config/bilingual_keywords.yaml

# ========== 注意事项 ==========
# - 不要将此 .env.example 文件重命名为 .env 后直接提交
# - 真实的 .env 文件已在 .gitignore 中
# - 如需更改集合，只需修改 TEMP_COLLECTION_KEY